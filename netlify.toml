[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[build.caching]
  enabled = true

[[plugins]]
package = "@netlify/plugin-lighthouse"
  [plugins.inputs]
  output_path = "lighthouse-report.html"
  audit_url = "/"  # Use "audit_url" instead of "url"